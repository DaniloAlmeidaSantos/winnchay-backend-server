const db = require('../config/db_config');

class NotificationRepository {

    async save() {
        db.insert(
            {

            }
        )
    }

}

module.exports = NotificationRepository;