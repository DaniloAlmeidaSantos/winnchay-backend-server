const Log4js = require('log4js');
var logger = Log4js.getLogger();
logger.level = "info";
class ClubsParticipateService {

    async clubsParticpate() {
        try {
            
        } catch (error) {
            throw new Error(`Error for find `);
        }
    }
}

module.exports = ClubsParticipateService;